const routes = {
    home: '/',
    login: '/login',
    detailsMovie: '/details/:media_type/:id',
    watch: '/watch/:media_type/:id',
    watchTv: '/watch/:media_type/:id/season/:season/esp/:esp',
    search: '/search',
    viewMore: '/:media_type/:type',
};

export default routes;
